module Colors = {
  let primary = #hex("DD1350") // #DD1350
  let black = #hex("0E0E0E") // #0E0E0E
  let white = #hex("FAFAFA") //#FAFAFA
}

module Spacing = {
  let xxsmall = #rem(0.5)
  let xsmall = #rem(1.0)
  let small = #rem(1.5)
  let medium = #rem(2.0)
  let large = #rem(2.5)
  let xlarge = #rem(3.0)
}
